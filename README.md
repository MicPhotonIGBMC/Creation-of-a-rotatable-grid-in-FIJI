# Creation-of-a-rotatable-grid-in-FIJI
the code allows you to draw a rectangle (with rotation) on an area defined by the user. This zone is cropped then divided into number of sectors defined by the user
